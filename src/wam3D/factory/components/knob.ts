import {BaseComponents} from "./baseComponents";
import {MeshLoader} from "../../meshLoader";
import {AbstractMesh, Mesh} from "@babylonjs/core";
export class Knob extends BaseComponents {
    constructor(label: string, options: any) {
        super(label, options);
    }
}
